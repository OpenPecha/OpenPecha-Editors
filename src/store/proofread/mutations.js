export function setVolumes(state, volumes) {
    state.volumes = volumes
}

export function setPageIds(state, pageIds) {
    state.pageIds = pageIds
}

export function setPageContent(state, pageContent) {
    state.page.content = pageContent
}

export function setPageImageUrl(state, pageImageUrl) {
    state.page.imageUrl = pageImageUrl
}